
import readme from '../README.md'

<% demos.forEach(function(demo, index) { %>
import Component<%=index%> from '../demos/<%=demo.name%>'
import config<%=index%> from '../demos/<%=demo.name%>/config'
config<%=index%>.story.content = Component<%=index%>
<% }) %>

export default () => {
  return {
    name: 'MainRightLayout',
    'stories': [
<% demos.forEach(function(demo, index) { %>
      config<%=index%>,
<% }) %>
    ],
    readme
  }
}
