
import readme from '../README.md'
import os from 'object-assign'
<% examples.forEach(function(demo, index) { %>
import Component<%=index%> from '../examples/<%=demo.name%>'
<% if (demo.hasEditableProps) { %>
import editableProps<%=index%> from '../examples/<%=demo.name%>/editable-props'
<% } %>
<% if (demo.hasDoc) { %>
import doc<%=index%> from '../examples/<%=demo.name%>/doc.md'
<% } %>
<% }) %>
export default{
  name: "<%=cmpName%>",
  'stories': [
<% examples.forEach(function(demo, index) { %>
    {
      name: '<%=demo.name%>',
      story: os({
        'content': Component<%=index%>,
<% if (demo.hasEditableProps) { %>
        'editableProps': editableProps<%=index%>
<% } else { %>
        'editableProps': []
<% } %>,
<% if (demo.hasDoc) { %>
        'doc': doc<%=index%>
<% } %>
      })
    },
<% }) %>
  ],
  readme
}
