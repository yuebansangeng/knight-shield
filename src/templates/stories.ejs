
export default {
  'name': require('<%=cpath%>/.bscpmrc').name,
  'stories': [
<% examples.forEach(function(demo, index) { %>    {
      'name': '<%=demo.name%>',
      'story': Object.assign({
        'content': require('<%=cpath%>/examples/<%=demo.name%>'),
        'editableProps': <% if (demo.hasEditableProps) { %>require('<%=cpath%>/examples/<%=demo.name%>/editable-props'),<% } else { %>[],<% } %><% if (demo.hasDoc) { %>
        'doc': require('<%=cpath%>/examples/<%=demo.name%>/doc.md')<% } %>
      })
    },<% }) %>
  ],
  'readme': require('<%=cpath%>/README.md')
}
