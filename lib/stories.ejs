
export default {
  'name': '<%= storyMeta.name %>',
  'stories': [
    <% storyMeta.stories.forEach(example => { %>
      'name': '<%= example.name %>',
      'story': {
        'component': '<%- example.component %>'
      }
    <% }) %>
  ],
  'readme': '<%= storyMeta.readme %>'
}
