
export default {
  'name': '<%=name%>',
  'stories': [
<% examples.forEach((demo, index) => { %>    {
      'name': '<%=demo.name%>',
      'story': Object.assign({
        'component': require('<%=cpath%>/examples/<%=demo.name%>'),
      })
    },<% }) %>
  ],
  'readme': require('<%=cpath%>/README.md')
}
