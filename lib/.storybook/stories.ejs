
export default {
  'name': '<%=name%>',
  'stories': [
<% examples.forEach((demo, index) => { %>    {
      'name': '<%=demo.name%>',
      'story': Object.assign({
        'component': require('<%=cpath%>/examples/<%=demo.name%>'),
      })
    },<% }) %>
  ],
  'readme': <% if(hasReadme){ %>require('<%=cpath%>/README.md')<% }else{ %>''<% } %>
}
